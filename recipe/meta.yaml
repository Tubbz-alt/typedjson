package:
  name: typed_json
  version: "0.0.1"

source:
  git_url: https://github.com/slac-lcls/typedjson.git
  git_tag: "0.0.1"

build:
  noarch: python
  script:
    - pip install --no-deps --disable-pip-version-check .

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - setuptools

about:
  home: https://github.com/slaclab/flask_authzn
  license: MIT License
